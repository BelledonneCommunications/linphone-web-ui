/**
 * Contacts engine Skeleton
 */

linphone.models.contacts.skeleton = {
	/*
	 * Object
	 */
	object: {
	},
	
	/* 
	 * Engine
	 */
	engine: function() {
		
	}
}

//
// List
//

linphone.models.contacts.skeleton.engine.prototype.count = function() {
};
 
linphone.models.contacts.skeleton.engine.prototype.list = function(filters) { 	
};


//
// CRUD
//
 
linphone.models.contacts.skeleton.engine.prototype.read = function(id) {
};
 		
linphone.models.contacts.skeleton.engine.prototype.create = function(object) {
};
 
linphone.models.contacts.skeleton.engine.prototype.update = function(id, object) {
};

linphone.models.contacts.skeleton.engine.prototype.remove = function(id) {
};